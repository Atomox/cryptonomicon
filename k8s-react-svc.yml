apiVersion: v1
kind: Service
metadata:
  name: crypto-react-svc
  labels:
    app: crypto-react
spec:
  type: NodePort
  ports:
    - port: 8042
      nodePort: 30002
      protocol: TCP
  selector:
    app: crypto-react
    tier: Frontend
    version: "1.1"
